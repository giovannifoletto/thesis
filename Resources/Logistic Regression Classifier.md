Take in the log combination of one or more independent variables.

The logistic regression (or logit regression) is estimating the parameters of a logistic model (the coefficient in the linear combination).
